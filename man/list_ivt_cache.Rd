% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{list_ivt_cache}
\alias{list_ivt_cache}
\title{List the canivt cache contents}
\usage{
list_ivt_cache(catalogue = NULL)
}
\arguments{
\item{catalogue}{A catalogue tibble (from \code{\link[=statcan_ivt_catalogue]{statcan_ivt_catalogue()}}) used to
enrich the listing. \code{NULL} (default) reads the \strong{cached} catalogue if one
exists and skips enrichment otherwise -- it never triggers a scrape, so this
function works offline.}
}
\value{
A tibble with one row per cached file: \code{kind} (\code{"ivt"} or
\code{"parquet"}), \code{key} (the cache key -- the catalogue number for downloaded
tables, the folder/file name otherwise), \code{language} (\code{"en"}/\code{"fr"} for a
language-marked Parquet, \code{NA} for \code{.ivt} files and old unmarked Parquets),
\code{path}, \code{bytes}, \code{modified}, and the catalogue columns \code{catalogue}, \code{title},
\code{census_year}, \code{topic} (\code{NA} when the key matches no product).
}
\description{
Lists the raw \code{.ivt} inputs (in the ivt cache) and the parsed data Parquets
(in the data cache), one row per file, enriched with catalogue metadata
(matched product number, title, census year, topic) when the file's cache key
matches a product in the \code{\link[=statcan_ivt_catalogue]{statcan_ivt_catalogue()}}. The member sidecars
(\verb{_members.parquet}) and the catalogue cache itself are infrastructure and are
not listed.
}
\examples{
# Lists whatever is in the configured cache directories (empty when unset):
list_ivt_cache()
}
\seealso{
\code{\link[=get_statcan_ivt]{get_statcan_ivt()}}, \code{\link[=statcan_ivt_catalogue]{statcan_ivt_catalogue()}}
}
