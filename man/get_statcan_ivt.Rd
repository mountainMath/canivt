% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{get_statcan_ivt}
\alias{get_statcan_ivt}
\title{Get a StatCan IVT table as a Parquet connection}
\usage{
get_statcan_ivt(
  catalogue,
  geo_attributes = FALSE,
  labels = TRUE,
  dim_names = c("slug", "label"),
  language = "en",
  keep_ivt = FALSE,
  refresh = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{catalogue}{A StatCan catalogue number (e.g. \code{"98-10-0241-01"},
\code{"95F0436XCB2001003"}), a Borealis id (a filename like \code{"CD1T29M3"}, its
DOI-qualified key like \code{"SP3/6DGOTF/CD1T29M3"}, or a numeric \code{file_id}), or a
custom identifier matching a local \code{.ivt} in the ivt cache. Matching is case-
and punctuation-insensitive. A bare Borealis filename that occurs in several
datasets is ambiguous and aborts with the disambiguating keys. \strong{A one-row
tibble from \code{\link[=statcan_ivt_catalogue]{statcan_ivt_catalogue()}} or \code{\link[=borealis_ivt_catalogue]{borealis_ivt_catalogue()}}} may be
passed instead of an id, routing straight to that row's source with no
catalogue lookup. A \strong{named} length-one vector/list (\code{c(key = "url-or-path")})
is a manual import: its name is the cache key and its value a URL or local
file path to a zipped or raw \code{.ivt}.}

\item{geo_attributes}{Passed to \code{\link[=read_ivt]{read_ivt()}}: decode the full family-2
geography attribute table (slower) so geographies can be labelled by name.}

\item{labels}{Passed to \code{\link[=ivt_write_parquet]{ivt_write_parquet()}}: write labelled columns
(\code{TRUE}, default) or the compact integer-id table.}

\item{dim_names}{Passed to \code{\link[=ivt_write_parquet]{ivt_write_parquet()}}: name the data-dimension
columns by the full dimension label (\code{"label"}, default) or the terse
structural slug (\code{"slug"}).}

\item{language}{Passed to \code{\link[=ivt_write_parquet]{ivt_write_parquet()}}: output labels and
label-derived column names in English (\code{"en"}, default) or French (\code{"fr"}).}

\item{keep_ivt}{Persist the downloaded/imported raw \code{.ivt} in the ivt cache
(\link[=ivt_cache_dir]{ivt_cache_dir("ivt")}). The default \code{FALSE} decodes the
\code{.ivt} from a temporary copy and then discards it, keeping only the parsed
Parquet (re-fetched if the Parquet is later rebuilt). A raw \code{.ivt} already
persisted in the ivt cache is reused regardless of this flag.}

\item{refresh}{Re-download and re-parse even if cached outputs exist.}

\item{quiet}{Suppress progress messages.}
}
\value{
An \code{\link[arrow:open_dataset]{arrow::open_dataset()}} connection to the Parquet file. The Parquet
path is attached as \code{attr(., "path")}; the resolved catalogue row (if any)
as \code{attr(., "catalogue_row")}; the member-level table (\code{\link[=ivt_members]{ivt_members()}},
read from the \verb{_members.parquet} sidecar when present) as
\code{attr(., "members")} -- \code{\link[=collect_ivt]{collect_ivt()}} uses it to convert dimension
columns into full-level factors.
}
\description{
One-stop accessor: given a table id, this resolves the product (the
\code{\link[=statcan_ivt_catalogue]{statcan_ivt_catalogue()}} first, then the \code{\link[=borealis_ivt_catalogue]{borealis_ivt_catalogue()}} of custom
tabulations), downloads its \code{.ivt} (cached in the ivt cache), decodes it with
\code{\link[=read_ivt]{read_ivt()}}, writes the tidy table to Parquet (cached in the data cache) and
returns an Arrow connection to that Parquet so the data can be queried lazily
(e.g. with \code{dplyr}) without loading it all into memory.
}
\details{
\code{catalogue} may also be a \strong{custom identifier} for an \code{.ivt} file you have
placed in the ivt cache yourself (as \verb{<id>.ivt} directly in
\link[=ivt_cache_dir]{ivt_cache_dir("ivt")}, or as the only \code{.ivt} in a \verb{<id>/}
subfolder). Such files are used directly, with no catalogue lookup or
download -- handy for tables that are not on either index, or for local
experiments.

For an ad-hoc table on neither index, pass a \strong{named} length-one vector or
list whose name is the local cache key and whose value is a URL or a local
file path to a (zipped or raw) \code{.ivt}:
\code{get_statcan_ivt(c(my_table = "~/Downloads/foo.zip"))}. The name keys the
cached Parquet; the value is fetched (URL) or copied in place (local path,
never moved), sniffed so a \code{.zip} is unzipped and a raw \code{.ivt} used as-is,
then decoded.
}
\seealso{
\code{\link[=statcan_ivt_catalogue]{statcan_ivt_catalogue()}}, \code{\link[=read_ivt]{read_ivt()}}
}
