% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{ivt_cache_dir}
\alias{ivt_cache_dir}
\title{canivt cache directories}
\usage{
ivt_cache_dir(which = c("ivt", "data"), create = TRUE)
}
\arguments{
\item{which}{Which cache to resolve: \code{"ivt"} (raw downloads) or \code{"data"}
(parsed Parquet + metadata).}

\item{create}{Create the directory if it does not exist? Default \code{TRUE}.}
}
\value{
The cache directory path (a \code{tempdir()} subfolder when the option is
unset).
}
\description{
canivt uses two optional cache locations, each controlled by an R option (or
the matching environment variable, which is read into the option when the
package loads so it can be set in \code{.Renviron}):
}
\details{
\itemize{
\item \strong{\code{ivt}} -- raw downloaded \code{.ivt} files, option \code{canivt.ivt_cache} (env var
\code{CANIVT_IVT_CACHE}). When unset, downloads go to a per-session \code{\link[=tempdir]{tempdir()}}
folder and are discarded at the end of the session.
\item \strong{\code{data}} -- parsed Parquet data and metadata that should persist across
sessions, option \code{canivt.data_cache} (env var \code{CANIVT_DATA_CACHE}). When
unset, parsed output goes to \code{\link[=tempdir]{tempdir()}} (and is lost when the session ends;
a one-time startup message points this out).
}

Set them either as options (e.g. in \code{.Rprofile}):
\code{options(canivt.data_cache = "~/canivt-cache")}, or as environment variables
(e.g. in \code{.Renviron}): \verb{CANIVT_DATA_CACHE=~/canivt-cache}.
}
\examples{
# With no option set, the cache resolves to a per-session tempdir folder.
ivt_cache_dir("data", create = FALSE)
}
