% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{prune_ivt_cache}
\alias{prune_ivt_cache}
\title{Prune files from the canivt cache}
\usage{
prune_ivt_cache(
  x = NULL,
  kind = NULL,
  language = NULL,
  sidecars = TRUE,
  dry_run = FALSE
)
}
\arguments{
\item{x}{A character vector of catalogue numbers / cache keys, \strong{or} a data
frame from \code{\link[=list_ivt_cache]{list_ivt_cache()}} (its \code{path} column is used), or \code{NULL} (all
listed files, subject to \code{kind}/\code{language}).}

\item{kind}{Restrict to \code{"ivt"} and/or \code{"parquet"} files. \code{NULL} = both.}

\item{language}{Restrict Parquets to these languages (\code{"en"}/\code{"fr"}); \code{.ivt}
files (language \code{NA}) are excluded when \code{language} is set. \code{NULL} = all.}

\item{sidecars}{Also delete a \verb{<key>_members.parquet} sidecar once no Parquet
references it (default \code{TRUE}).}

\item{dry_run}{If \code{TRUE}, report what would be removed without deleting.}
}
\value{
Invisibly, a tibble of the removed (or, for \code{dry_run}, matched) files
with \code{kind} (\code{"ivt"}/\code{"parquet"}/\code{"sidecar"}), \code{path} and \code{bytes}.
}
\description{
Deletes raw \code{.ivt} inputs and/or parsed data Parquets from the cache. The
target files can be given three ways (combinable):
}
\details{
\itemize{
\item \strong{catalogue numbers / cache keys} via \code{x} (a character vector), matched as
in \code{\link[=list_ivt_cache]{list_ivt_cache()}} (normalised key or catalogue number, exact or prefix);
\item \strong{format / language filters} via \code{kind} (\code{"ivt"} / \code{"parquet"}) and
\code{language} (\code{"en"} / \code{"fr"});
\item a \strong{filtered listing} via \code{x} (a data frame from \code{\link[=list_ivt_cache]{list_ivt_cache()}}, e.g.
\code{list_ivt_cache() |> dplyr::filter(census_year == 2016)}): exactly its \code{path}
files are removed.
}

With no \code{x}/\code{kind}/\code{language}, \strong{every} listed cache file is targeted (the
member sidecars and the catalogue cache are never touched here). When a
Parquet is removed and no remaining Parquet references its shared
\verb{<key>_members.parquet} sidecar, the orphaned sidecar is removed too
(\code{sidecars = TRUE}); emptied per-table \code{.ivt} folders are cleaned up.
}
\examples{
# Preview what a prune would remove without deleting anything:
prune_ivt_cache(dry_run = TRUE)
}
\seealso{
\code{\link[=list_ivt_cache]{list_ivt_cache()}}
}
