% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{statcan_ivt_catalogue}
\alias{statcan_ivt_catalogue}
\title{Catalogue of StatCan Beyond 20/20 IVT products}
\usage{
statcan_ivt_catalogue(temporal = NULL, refresh = FALSE, quiet = FALSE)
}
\arguments{
\item{temporal}{Optional character/numeric vector of census versions to
return (the \code{temporal} values from \code{\link[=statcan_ivt_years]{statcan_ivt_years()}}, e.g. \code{2001} or
\code{c(2021, 2016)}). When \code{NULL} (default) the whole catalogue is returned.
Filtering does not avoid building the full cache the first time.}

\item{refresh}{Re-scrape the index even if a cached catalogue exists.}

\item{quiet}{Suppress per-page progress messages.}
}
\value{
A tibble with columns \code{census_year}, \code{temporal}, \code{catalogue},
\code{archived}, \code{release_date}, \code{date}, \code{topic}, \code{title}, \code{pid}, \code{ivt_url},
\code{download_url} and \code{http_url}. \code{ivt_url} is the link as published (the
\code{Alternative.cfm} intermediate page or a direct b2020 \code{.zip});
\code{download_url} is the resolved direct-download URL (see
\code{\link[=statcan_ivt_resolve_url]{statcan_ivt_resolve_url()}}).
}
\description{
Scrapes the StatCan census datasets index into a tidy catalogue of every IVT
product: census year, catalogue number, release date, topic, title and the
download URLs. The full catalogue (all census versions) is cached as a
Parquet file in the data cache (\link[=ivt_cache_dir]{ivt_cache_dir("data")}) so it
is scraped only once; pass \code{refresh = TRUE} to rebuild it. When the cached
catalogue is a month or more old a warning (once per session) suggests
refreshing it.
}
