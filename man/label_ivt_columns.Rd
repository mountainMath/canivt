% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect.R
\name{label_ivt_columns}
\alias{label_ivt_columns}
\title{Relabel slug data columns with their full dimension names}
\usage{
label_ivt_columns(x, members = NULL, language = NULL)
}
\arguments{
\item{x}{An Arrow dataset, a dplyr-on-Arrow query, or a data frame.}

\item{members}{A level table from \code{\link[=ivt_members]{ivt_members()}}; when \code{NULL} it is located
from \code{x} (attached \code{members} attribute or the \verb{_members.parquet} sidecar).}

\item{language}{\code{"en"} or \code{"fr"}; \code{NULL} (default) auto-detects from the file
name (see \code{\link[=ivt_parquet_language]{ivt_parquet_language()}}).}
}
\value{
\code{x} with its data-dimension columns renamed (an Arrow query when \code{x}
is an Arrow object, else a data frame).
}
\description{
The Parquet written by \code{\link[=ivt_write_parquet]{ivt_write_parquet()}} / \code{\link[=get_statcan_ivt]{get_statcan_ivt()}} names its
data-dimension columns by their compact structural slug (\code{age}, \code{tenure}, ...).
This renames those columns to the full dimension label -- English or French --
on an Arrow dataset / dplyr-on-Arrow query (lazily, no data read) or a
collected data frame. Geography columns (\code{geo_name}, \code{geo_uid}, ...) are left as
is; a column whose slug is not found is skipped.
}
\details{
The slug -> label map comes from the \verb{<name>_members.parquet} sidecar (or an
explicit \code{members} table, e.g. from \code{\link[=ivt_members]{ivt_members()}}). The language is taken
from the file name marker via \code{\link[=ivt_parquet_language]{ivt_parquet_language()}} unless given, so the
labels match the language the Parquet was written in.

It can be called either side of \code{\link[=collect_ivt]{collect_ivt()}}: before, on the connection
(nothing is read until the collect), or after, on the collected data frame --
which carries the member table it used as an attribute. Either order gives
the same labelled, fully-levelled result, and \code{collect_ivt(dim_names = "label")} is the one-call shorthand for it.
}
\examples{
path <- system.file("extdata", "98100044.ivt", package = "canivt")
ivt <- read_ivt(path)
if (requireNamespace("dplyr", quietly = TRUE)) {
  df <- ivt_tidy(ivt, labels = FALSE)          # slug-named columns
  labelled <- label_ivt_columns(df, members = ivt_members(ivt))
  names(labelled)
}
}
\seealso{
\code{\link[=collect_ivt]{collect_ivt()}}, \code{\link[=ivt_parquet_language]{ivt_parquet_language()}}
}
