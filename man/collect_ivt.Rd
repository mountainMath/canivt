% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect.R
\name{collect_ivt}
\alias{collect_ivt}
\title{Collect IVT data with dimension columns as factors}
\usage{
collect_ivt(x, members = NULL, geography = FALSE, ...)
}
\arguments{
\item{x}{An \code{ivt} object, an Arrow dataset / dplyr-on-Arrow query, or a
\code{.parquet} path.}

\item{members}{A level table from \code{\link[=ivt_members]{ivt_members()}}; when \code{NULL} it is located
from \code{x} (an attached \code{members} attribute, or the Parquet's
\verb{_members.parquet} sidecar).}

\item{geography}{Also convert the geography columns (\code{geo_label},
\code{geo_name}, \code{geo_uid}, \code{geo_level}) to factors. Default \code{FALSE}: large
tables carry tens of thousands of geographies, which makes for unwieldy
factor levels.}

\item{...}{For \code{ivt} objects, passed to \code{\link[=ivt_tidy]{ivt_tidy()}}.}
}
\value{
A tibble with the dimension columns converted to factors.
}
\description{
Like \code{dplyr::collect()}, but converts each labelled dimension column into a
factor whose levels are the table's \strong{full} member list in codebook
member-ordinal order -- so members filtered out of the collected rows are
still visible as factor levels, and members always sort in their StatCan
order rather than alphabetically.
}
\details{
\code{x} can be an \code{ivt} object from \code{\link[=read_ivt]{read_ivt()}}, the Arrow dataset returned by
\code{\link[=get_statcan_ivt]{get_statcan_ivt()}} (optionally after \code{dplyr} verbs such as \code{filter()}), or
a path to a Parquet written by \code{\link[=ivt_write_parquet]{ivt_write_parquet()}}. For the Arrow /
Parquet forms the member levels come from the \verb{<name>_members.parquet}
sidecar written alongside the data (or pass \code{members} explicitly, e.g. from
\code{\link[=ivt_members]{ivt_members()}}). Works on both the labelled table and the compact
integer-id table (\code{labels = FALSE}), where the member-id columns are mapped
to their labels while being converted.
}
