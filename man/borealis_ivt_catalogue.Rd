% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borealis.R
\name{borealis_ivt_catalogue}
\alias{borealis_ivt_catalogue}
\title{Catalogue of Borealis Dataverse Beyond 20/20 IVT products}
\usage{
borealis_ivt_catalogue(refresh = FALSE, quiet = FALSE)
}
\arguments{
\item{refresh}{Re-run the live search even if a cached catalogue exists.}

\item{quiet}{Suppress per-batch progress messages.}
}
\value{
A tibble with one row per \code{.ivt} file: \code{key} (the canonical
\verb{<doi-token>/<file-stem>} address), \code{file} (the deposited filename),
\code{file_stem}, \code{file_id} (the numeric Borealis datafile id -- always unique),
\code{dataset_doi}, \code{dataset_name}, \code{download_url}, \code{size_in_bytes}, \code{md5},
\code{published_at} and \code{file_type}.
}
\description{
Searches the \href{https://borealisdata.ca}{Borealis Dataverse} for every deposited
\code{.ivt} file and returns a tidy catalogue. Borealis hosts custom StatCan
tabulations that complement the official \code{\link[=statcan_ivt_catalogue]{statcan_ivt_catalogue()}}; the two are
used together by \code{\link[=get_statcan_ivt]{get_statcan_ivt()}}, which resolves a table against StatCan
first and falls back to Borealis.
}
\details{
The search is slow (~90 s, thousands of files) so the full catalogue is cached
as a Parquet file in the data cache (\link[=ivt_cache_dir]{ivt_cache_dir("data")});
pass \code{refresh = TRUE} to rebuild it. Reading the cache needs neither the
\code{dataverse} package nor an API key -- only a live search does.

A \strong{Borealis API key} is required to run the search. Get one from your
Borealis account (\url{https://borealisdata.ca/dataverseuser.xhtml?selectTab=apiTokenTab})
and set it as the environment variable \code{BOREALIS_DATAVERSE_KEY} (e.g. in
\code{.Renviron}).
}
\seealso{
\code{\link[=statcan_ivt_catalogue]{statcan_ivt_catalogue()}}, \code{\link[=get_statcan_ivt]{get_statcan_ivt()}}, \code{\link[=borealis_ivt_download]{borealis_ivt_download()}}
}
