% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{ivt_write_parquet}
\alias{ivt_write_parquet}
\title{Write an IVT table to Parquet}
\usage{
ivt_write_parquet(
  x,
  path = NULL,
  labels = TRUE,
  members = TRUE,
  dim_names = c("slug", "label"),
  language = "en",
  ...
)
}
\arguments{
\item{x}{An \code{ivt} object from \code{\link[=read_ivt]{read_ivt()}}.}

\item{path}{Output \code{.parquet} path. Defaults to \verb{<product_id>.parquet} in the
data cache (\link[=ivt_cache_dir]{ivt_cache_dir("data")}, i.e. option
\code{canivt.data_cache} or \code{\link[=tempdir]{tempdir()}} when unset).}

\item{labels}{Passed to \code{\link[=ivt_tidy]{ivt_tidy()}}: write labelled columns (\code{TRUE},
default) or the compact integer-id table (\code{FALSE}).}

\item{members}{Also write the member-level table (\code{\link[=ivt_members]{ivt_members()}}) as a
\verb{<name>_members.parquet} sidecar next to \code{path} (\code{TRUE}, default), so
\code{\link[=collect_ivt]{collect_ivt()}} can convert dimension columns to full-level factors.}

\item{dim_names}{How to name the data-dimension columns (passed to
\code{\link[=ivt_tidy]{ivt_tidy()}} and the member sidecar): \code{"slug"} (default, the terse
structural slug) or \code{"label"} (the full dimension name). Slug columns can be
labelled on read with \code{\link[=label_ivt_columns]{label_ivt_columns()}}.}

\item{language}{Output language for labels and label-derived column names
(passed to \code{\link[=ivt_tidy]{ivt_tidy()}}): \code{"en"} (default) or \code{"fr"}. The member sidecar
carries both languages regardless.}

\item{...}{Passed to \code{\link[arrow:write_parquet]{arrow::write_parquet()}}.}
}
\value{
\code{path}, invisibly.
}
\description{
Write an IVT table to Parquet
}
\examples{
path <- system.file("extdata", "98100044.ivt", package = "canivt")
ivt <- read_ivt(path)
if (requireNamespace("arrow", quietly = TRUE)) {
  out <- ivt_write_parquet(ivt, file.path(tempdir(), "98100044.parquet"))
  file.exists(out)
}
}
