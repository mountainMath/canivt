% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{ivt_write_parquet}
\alias{ivt_write_parquet}
\title{Write an IVT table to Parquet}
\usage{
ivt_write_parquet(
  x,
  path = NULL,
  labels = TRUE,
  members = TRUE,
  missing = TRUE,
  dim_names = c("slug", "label"),
  language = "en",
  chunk_cells = getOption("canivt.chunk_cells", 5e+06),
  ...
)
}
\arguments{
\item{x}{An \code{ivt} object from \code{\link[=read_ivt]{read_ivt()}}, \strong{or the path to an \code{.ivt} file}
-- then the table is decoded and written a chunk at a time and the completed
grid is never held whole, so a table far larger than memory converts on a
small machine. The written file is the same one the in-memory path
produces. See \code{chunk_cells}.}

\item{path}{Output \code{.parquet} path. Defaults to \verb{<product_id>.parquet} in the
data cache (\link[=ivt_cache_dir]{ivt_cache_dir("data")}, i.e. option
\code{canivt.data_cache} or \code{\link[=tempdir]{tempdir()}} when unset).}

\item{labels}{Passed to \code{\link[=ivt_tidy]{ivt_tidy()}}: write labelled columns (\code{TRUE},
default) or the compact integer-id table (\code{FALSE}).}

\item{members}{Also write the member-level table (\code{\link[=ivt_members]{ivt_members()}}) as a
\verb{<name>_members.parquet} sidecar next to \code{path} (\code{TRUE}, default), so
\code{\link[=collect_ivt]{collect_ivt()}} can convert dimension columns to full-level factors.}

\item{missing}{Also write the cell-status table (\code{\link[=ivt_tidy_missing]{ivt_tidy_missing()}}) as a
\verb{<name>_missing.parquet} sidecar next to \code{path} (\code{TRUE}, default), when \code{x}
carries one -- i.e. when it was read with \code{read_ivt(missing = TRUE)}. The
data table holds only the cells that have a value; this sidecar is what
says of the rest which are genuine zeros (absent from both) and which are
missing, and why. Its coordinate columns are labelled exactly like the data
table's, so the two join. Silently skipped when \code{x$missing} is \code{NULL}.}

\item{dim_names}{How to name the data-dimension columns (passed to
\code{\link[=ivt_tidy]{ivt_tidy()}} and the member sidecar): \code{"slug"} (default, the terse
structural slug) or \code{"label"} (the full dimension name). Slug columns can be
labelled on read with \code{\link[=label_ivt_columns]{label_ivt_columns()}}.}

\item{language}{Output language for labels and label-derived column names
(passed to \code{\link[=ivt_tidy]{ivt_tidy()}}): \code{"en"} (default) or \code{"fr"}. The member sidecar
carries both languages regardless.}

\item{chunk_cells}{Only when \code{x} is a file path: how many grid rows to
decode, write and drop at a time (default \code{getOption("canivt.chunk_cells", 5e6)}). Chunks are cut along the outermost paged dimension -- usually
geography -- which is the axis the file itself pages on, so a chunk is a
contiguous run of output rows and no page is read twice. A table whose
layout pages on nothing but the straddle window cannot be sliced and is
held whole, subject to the usual \code{canivt.max_cells} guard.}

\item{...}{Passed to \code{\link[arrow:write_parquet]{arrow::write_parquet()}} (or, on the streaming path, to
\link[arrow:ParquetFileWriter]{arrow::ParquetFileWriter}\verb{$create()}).}
}
\value{
\code{path}, invisibly.
}
\description{
Write an IVT table to Parquet
}
\examples{
path <- system.file("extdata", "98100044.ivt", package = "canivt")
ivt <- read_ivt(path)
if (requireNamespace("arrow", quietly = TRUE)) {
  out <- ivt_write_parquet(ivt, file.path(tempdir(), "98100044.parquet"))
  file.exists(out)
  # the same file, without holding the completed table in memory
  ivt_write_parquet(path, file.path(tempdir(), "streamed.parquet"))
}
}
