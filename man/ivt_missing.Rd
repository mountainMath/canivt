% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect.R
\name{ivt_missing}
\alias{ivt_missing}
\title{Get the cell-status (missing-cell) table}
\usage{
ivt_missing(x)
}
\arguments{
\item{x}{An \code{ivt} object, a \code{.parquet} path, or an Arrow dataset / dplyr query
from \code{\link[=get_statcan_ivt]{get_statcan_ivt()}}.}
}
\value{
For an \code{ivt}, the \code{missing} tibble (member-id coordinates); for a
Parquet source, a lazy \code{\link[arrow:open_dataset]{arrow::open_dataset()}} connection to the
\verb{_missing.parquet} sidecar with the same labelled coordinates as the data.
\code{NULL} when the table carries none -- i.e. it was decoded without
\code{missing = TRUE}.
}
\description{
The one accessor for "which of the cells this table does not carry are
missing rather than zero, and why", whichever form the table is in: an \code{ivt}
object from \link[=read_ivt]{read_ivt(missing = TRUE)}, a Parquet path, or the
Arrow connection \code{\link[=get_statcan_ivt]{get_statcan_ivt()}} returns.
}
\details{
Only cells with a value are stored, so an absent cell is \emph{either} a published
zero or a missing value, and this table is the only thing that separates
them: every row is an absent cell the file marks as \strong{not} a zero, with the
reason the file states (\code{symbol} / \code{status}), or \code{NA} where the page carries
only the bare absent mask. Absent cells that appear in neither table are
genuine zeros.
}
\examples{
path <- system.file("extdata", "98100044.ivt", package = "canivt")
ivt_missing(read_ivt(path, missing = TRUE))
}
\seealso{
\code{\link[=read_ivt]{read_ivt()}}, \code{\link[=ivt_tidy_missing]{ivt_tidy_missing()}}, \code{\link[=ivt_write_parquet]{ivt_write_parquet()}}
}
