% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{read_ivt}
\alias{read_ivt}
\title{Read a Beyond 20/20 IVT file}
\usage{
read_ivt(path, geo_attributes = FALSE, missing = FALSE)
}
\arguments{
\item{path}{Path to an \code{.ivt} file.}

\item{geo_attributes}{For the large chunked family-2 tables only: if \code{TRUE},
decode the full geography attribute table (names, level/type, geocodes,
data-quality flag, non-response rate) from the codebook so \code{\link[=ivt_tidy]{ivt_tidy()}}
can label geographies by name. This adds a codebook block-scan (tens of
seconds); the default \code{FALSE} keeps those tables keyed by DGUID. Small
schema'd tables and the pre-DGUID (inline-codebook) tables already carry
their full attribute set on the default metadata path. Ignored for
family-1 tables.}

\item{missing}{If \code{TRUE} (default \code{FALSE}) also decode each page's \strong{cell
status tail} and return, in a \code{missing} tibble, the coordinates of the
cells the file marks as \emph{not available} rather than zero (same member-id
columns as \code{cells}, no \code{value}, plus \code{symbol} and \code{status} columns carrying
the reason \strong{in the file's own words} -- the legend it declares in its
header, e.g. \code{x} / "Suppressed to meet the confidentiality requirements of
the Statistics Act" -- or \code{NA} where the page carries only the bare absent
mask). The legend itself travels as \code{attr(x$missing, "legend")}. Off by
default because it costs a second presence read per page and its
completeness is vintage-dependent: pages with no tail at all, pages whose
tail the word index does not account for, and cells carrying a reason code
the file's legend does not name all contribute nothing -- each reported
with a classed warning rather than assumed to hold no missings. See the
"Missing values" section.}
}
\value{
An object of class \code{ivt}: a list with \code{cells} (a tibble of one value
per row, keyed by 1-based member-id columns matching the StatCan metadata
Member IDs), and \code{metadata} (table identity, \code{dimensions}, \code{geographies},
and \code{footnotes}). \code{metadata$geographies} carries, per member and where the
vintage stores them: the bilingual display label (\code{geo_label},
\code{geo_label_fr}) and name (\code{geo_name}, \code{geo_name_fr} -- on pre-DGUID tables
the EN/FR halves of the stored bilingual label), \code{geo_uid} (DGUID, or the
bare GEOUID on pre-DGUID tables), the aggregation level (\code{geo_level}), the
label hierarchy (\code{geo_depth}, \code{geo_parent_id} -- the indentation the display
label carries, turned into a depth and the \code{member_id} of each member's
nearest shallower ancestor; both absent when the geography axis is flat),
the geography type / municipal status (\code{geo_type}, \code{geo_type_abbr}), province
abbreviation and codes (\code{prov_abbr}, \code{alt_geo_code}, \code{pr_code}), the
data-quality flag (\code{dqf_code}, with \code{dqf_note} and the table-level
\code{dqf_legend}), and the total non-response rate (\code{tnr_short_form}).

Where \code{dqf_note} is present it is accompanied by a \code{dqf_note_truncated}
logical flag: StatCan's writer stores each note in a single-byte-length
record, so notes longer than 252 characters are truncated \strong{in the source
file} (2,448 of 63,404 geographies in 98-10-0129, 90 of 6,297 in
98-10-0478). The read is byte-exact -- this is a container limitation, not a
decode gap, and there is no continuation to recover -- but the flag marks the
affected members and a classed \code{canivt_dqf_note_truncated} warning is raised
so the loss is never silent. (Unlike a heuristic fallback it is \emph{not} upgraded
to an error by \code{options(canivt.strict = TRUE)}: the bytes are exactly what the
file holds.) The truncation is a container limit of the \code{.ivt} export only --
StatCan's authoritative metadata (the WDS \code{getCubeMetadata} \code{geoAttribute} or
the CSV-download metadata) stores the full untruncated note, so a consumer who
needs the complete text can recover it there.

Each footnote in \code{metadata$footnotes} carries a \code{scope} (\code{"table"},
\code{"dimension"} or \code{"member"}), the owning \code{dimension} name and, for member
notes, the \code{member_id}(s) it annotates -- \code{member_id} for a single member and
\code{member_refs} for the full set (geography counts as a dimension). This matches
StatCan's own footnote linkage on the modern tables; on the pre-DGUID profiles
the same linkage is recovered from the \code{(N)} reference markers embedded in the
member labels (a note there can be cited by many members, so \code{member_refs}
lists them all).

The value store keeps only non-zero cells, so a cell absent from \code{cells}
is a \strong{zero} -- \emph{within a geography that carries any data}. A geography
with no stored cells at all is either wholly suppressed or wholly empty,
and the cell store cannot distinguish the two: \code{metadata$geographies$has_data}
flags which geographies carry data, and on the pre-DGUID tables
\code{metadata$geographies$dqf_code} (the per-geography data-quality flag from
the codebook) corroborates it (e.g. on the 2016 income table
98-400-X2016120 the flag's last digit is \code{9} exactly for the 888
geographies with no stored cells, which the Beyond 20/20 viewer renders
as suppressed).
}
\description{
Parses a Statistics Canada Beyond 20/20 \code{.ivt} table straight from its bytes:
both the data cells and the codebook (dimension members, geographic
identifiers, footnotes). No companion CSV or metadata download is required.
}
\details{
Every primary read is positional (header pointers, block directories, framed
value entries). When one does not resolve and a content-heuristic fallback
supplies values instead -- or when directory entries point at page variants
that cannot be decoded -- a classed warning (\code{canivt_fallback} /
\code{canivt_skipped_pages}) is raised naming the affected read. Set
\code{options(canivt.strict = TRUE)} to turn these into errors: on a file layout
this package has not been validated against, the fallback paths are the ones
most likely to misread silently.
}
\section{Missing values}{

Only non-zero cells are stored, so absence covers \strong{both} genuine zeros
and true missings (\code{x} suppressed, \code{...}/\code{N} not available). The two are
separated by a block each page appends after its dense value run, in one of
two forms selected by the page marker: a 1-bit \strong{absent mask} -- a strict
subset of the absent cells, where masked means a genuine zero and
\emph{unmasked} means missing -- or a self-describing \strong{reason-code array}
carrying the \code{..} / \code{x} / \code{...} distinction itself. \code{missing = TRUE}
decodes both, at every code width; only the array states a reason, so
mask-derived rows carry \code{status = NA}.

The reason codes are \strong{numbered by the file, not by the format}: each
table declares its own legend -- symbol plus bilingual wording, in code
order -- and the same symbol sits at different codes in different lineages
(\code{x} is code 2 in the NDM census tables and code 3 in the 2016
\code{98-400-X} crosstabs). \code{status} is read from that declaration, so it names
whatever the file names, including symbols outside the census vocabulary
(\code{F} too unreliable to be published, \verb{0 s} rounded to zero, \verb{®} not
released yet, \code{z} frozen series). A code the legend does not name, or a
table that declares no legend, is reported rather than guessed at
(\code{canivt_status_code_unknown}, \code{canivt_status_legend}).

Two limits are reported rather than hidden. On float64 pages a mask word of
mostly-ones is NaN-shaped and the writer's x87 quieting overwrites one
status bit per such word \strong{in the source file}; those cells read as zeros
and cannot be recovered (\code{canivt_status_nan_quieted}). And on a few
2001/2006 vintages the page's word index also addresses a second, undecoded
block past the mask (\code{canivt_status_extra_block}).
}

\examples{
# A small real table (StatCan 98-10-0044) is bundled for examples/tests.
path <- system.file("extdata", "98100044.ivt", package = "canivt")
ivt <- read_ivt(path)
ivt
head(ivt$cells)
}
