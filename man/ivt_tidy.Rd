% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{ivt_tidy}
\alias{ivt_tidy}
\title{Tidy an \code{ivt} object into a labelled data frame}
\usage{
ivt_tidy(
  x,
  labels = TRUE,
  trim_labels = TRUE,
  dim_names = c("slug", "label"),
  language = "en",
  depth = FALSE
)
}
\arguments{
\item{x}{An \code{ivt} object from \code{\link[=read_ivt]{read_ivt()}}.}

\item{labels}{If \code{TRUE} (default) replace member-id columns with member
labels; if \code{FALSE} return the compact integer-id table (member ids).}

\item{trim_labels}{If \code{TRUE} (default) strip the hierarchy-indentation spaces
from member labels.}

\item{dim_names}{How to name the data-dimension columns: \code{"slug"} (default)
uses the terse structural slug (e.g. \code{age}), which is compact and
language-neutral; \code{"label"} uses the full dimension name (e.g.
\verb{Age of primary household maintainer}, or its French equivalent when
\code{language = "fr"}). Slug output can be labelled afterwards with
\code{\link[=label_ivt_columns]{label_ivt_columns()}}. The choice applies to both \code{labels} values.}

\item{language}{Output language for labels and label-derived column names:
\code{"en"} (default) or \code{"fr"}. Also accepts \code{"eng"}/\code{"fra"} and any case (it is
lower-cased). French falls back to English wherever the file carries no
French copy (e.g. the language-neutral \code{geo_uid}, or a dimension with no
French name).}

\item{depth}{If \code{TRUE} (default \code{FALSE}) add a \verb{<col>_depth} integer column
after each data-dimension column giving that member's hierarchy depth (read
from the label indentation, the same measure carried by \code{\link[=ivt_members]{ivt_members()}}).
Opt-in, so the default output -- and hence the Parquet written by
\code{\link[=ivt_write_parquet]{ivt_write_parquet()}} -- is unchanged.}
}
\value{
A tibble.
}
\description{
Joins the decoded cells to the codebook so each dimension column holds its
member label (and geography gains a \code{dguid} column), producing a long table
analogous to the StatCan CSV download.
}
\examples{
path <- system.file("extdata", "98100044.ivt", package = "canivt")
ivt <- read_ivt(path)
ivt_tidy(ivt)
ivt_tidy(ivt, dim_names = "label")
}
