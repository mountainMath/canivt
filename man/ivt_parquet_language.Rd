% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect.R
\name{ivt_parquet_language}
\alias{ivt_parquet_language}
\title{Detect the language of an IVT Parquet}
\usage{
ivt_parquet_language(x)
}
\arguments{
\item{x}{A \code{.parquet} path, an Arrow dataset, or a dplyr-on-Arrow query (the
path is taken from the \code{path} attribute / source file list).}
}
\value{
\code{"en"} or \code{"fr"}.
}
\description{
Reads the language marker (\verb{_en} / \verb{_fr} before \code{.parquet}) that
\code{\link[=ivt_write_parquet]{ivt_write_parquet()}} / \code{\link[=get_statcan_ivt]{get_statcan_ivt()}} put in the file name, so
\code{\link[=collect_ivt]{collect_ivt()}} and \code{\link[=label_ivt_columns]{label_ivt_columns()}} can pick the matching language
without being told. Falls back to \code{"en"} when there is no marker.
}
