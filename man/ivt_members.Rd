% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect.R
\name{ivt_members}
\alias{ivt_members}
\title{Member levels of an IVT table}
\usage{
ivt_members(
  x,
  trim_labels = TRUE,
  dim_names = c("label", "slug"),
  language = "en"
)
}
\arguments{
\item{x}{An \code{ivt} object from \code{\link[=read_ivt]{read_ivt()}}.}

\item{trim_labels}{Trim the hierarchy-indentation whitespace from \code{level}
the same way \code{\link[=ivt_tidy]{ivt_tidy()}} does by default (\code{TRUE}).}

\item{dim_names}{How the data-dimension \code{column} names are formed, matching
\code{\link[=ivt_tidy]{ivt_tidy()}}: \code{"label"} (default, the full dimension name) or \code{"slug"} (the
terse structural slug). Must match the tidy output the levels will be joined
to.}

\item{language}{Language for the \code{column} names (label mode), matching
\code{\link[=ivt_tidy]{ivt_tidy()}}: \code{"en"} (default) or \code{"fr"}. The table always carries both the
English \code{level} and the French \code{level_fr}, so a single sidecar serves both
languages; only the label-derived \code{column} names follow \code{language}.}
}
\value{
A tibble with columns \code{column} (the tidy column name), \code{dimension}
(the full dimension name; \code{"Geography"} for the geography columns),
\code{member_id} (1-based StatCan member id), \code{ordinal} (the codebook
member-ordinal; equals \code{member_id} when the file stores no ordinal block),
\code{label} (the stored label, untrimmed), \code{level} (the label as it appears in
the tidy output), \code{level_fr} (the French label, \code{NA} when the file carries
none for that column) and \code{depth} (hierarchy depth implied by the label
indentation).
}
\description{
Returns one row per (column, member) for every labelled column that
\code{\link[=ivt_tidy]{ivt_tidy()}} emits: each data dimension (with its stored member-ordinal
order) and the geography columns. This is the level table \code{\link[=collect_ivt]{collect_ivt()}}
uses to convert dimension columns into factors whose levels cover \strong{all}
members -- including members filtered out of the data -- and it is written
next to the cached Parquet by \code{\link[=ivt_write_parquet]{ivt_write_parquet()}} / \code{\link[=get_statcan_ivt]{get_statcan_ivt()}}
(as \verb{<name>_members.parquet}).
}
\seealso{
\code{\link[=collect_ivt]{collect_ivt()}}
}
