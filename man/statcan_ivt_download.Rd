% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{statcan_ivt_download}
\alias{statcan_ivt_download}
\title{Download a StatCan IVT by its direct-download URL}
\usage{
statcan_ivt_download(
  download_url,
  key = NULL,
  dest_dir = NULL,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{download_url}{A direct-download URL (a b2020 \code{.zip} or a
\verb{Download.cfm?PID=} endpoint), \strong{or} a one-row \code{\link[=statcan_ivt_catalogue]{statcan_ivt_catalogue()}}
tibble (its \code{download_url} and, by default, its \code{catalogue} number as the
cache key are used).}

\item{key}{Cache key used to name the per-table folder under the ivt cache.
Optional when \code{download_url} is a catalogue row (defaults to the row's
catalogue number).}

\item{dest_dir}{Directory the \code{.ivt} is written to. Defaults to a per-table
folder under the ivt cache (\link[=ivt_cache_dir]{ivt_cache_dir("ivt")});
\code{\link[=get_statcan_ivt]{get_statcan_ivt()}} passes a temporary folder when \code{keep_ivt = FALSE}.}

\item{overwrite}{Re-download even if a \code{.ivt} already exists.}

\item{quiet}{Suppress the download message.}
}
\value{
Path to the local \code{.ivt} file.
}
\description{
Downloads a \code{.ivt} (or its containing \code{.zip}) from a resolved
\code{\link[=statcan_ivt_resolve_url]{statcan_ivt_resolve_url()}} URL into the ivt cache and returns the local
\code{.ivt} path. The payload is sniffed: a \code{.zip} is unzipped, a raw IVT is kept
as-is.
}
